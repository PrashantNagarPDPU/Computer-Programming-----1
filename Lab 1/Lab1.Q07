#include <stdio.h>

void main()
{
    float minutes, hours;

    printf("Enter time in minutes: ");
    scanf("%f", &minutes);

    hours = minutes / 60;     

    printf("%f minutes = %f hour(s) ", minutes, hours);
}