#include <stdio.h>

void main()
 {
    int n;
    printf("Enter the number of even terms to print: ");
    scanf("%d", &n);

    for (int i = 1; i <= n; i++)
     {
        printf("%d ", 2 * i);
    }
}