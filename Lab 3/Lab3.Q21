#include <stdio.h>

void main()
 {
int num, digit;

printf("Enter an integer: ");
scanf("%d", &num);

printf("Individual digits are: ");
while (num > 0)
 {
digit = num % 10;
printf("%d ", digit);
num /= 10;
}
printf("\n");
}